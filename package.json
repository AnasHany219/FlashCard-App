{
  "name": "flashcard-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "sass -w src/:public/css --style expanded --no-source-map && postcss dist/css/*.css --output dist/css --no-map --config"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.76.0"
  }
}
